import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
  },
  mainView: {
    flex: 1,
    width: "100%",
    paddingTop: 28,
  },
});

export default styles;
